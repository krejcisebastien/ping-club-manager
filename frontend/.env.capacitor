# Build de l'app native (npm run build:mobile) : l'app embarquée ne tourne pas
# sur le domaine du site, elle doit donc viser l'API de production en absolu.
VITE_API_URL=https://ping-club-manager-api.onrender.com/api
VITE_CLUB_NAME=Club Tennis de Table
